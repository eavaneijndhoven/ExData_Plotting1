## Instructions

- `makePlots.R` runs all other files (data file and plot files), and returns the png for each plot
- `data.R` loads the data, and cleans it
- `plot1.R`, `plot2.R`, `plot3.R`, `plot4.R`create all four plots and exports them as png's
- `plot1.png`, `plot2.png`, `plot3.png`, `plot4.png`show all four plots
